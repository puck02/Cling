format_version = 1

welcome_message = """
欢迎来到 Cling！🎯

这是一个交互式C语言学习系统，包含58道精心设计的练习题，
帮助你快速掌握C语言基础知识。

学习方式：
  1. 每道题都有TODO注释指引你需要完成的内容
  2. 修改练习文件后，系统会自动编译和测试
  3. 如果遇到困难，使用 'h' 键查看提示
  4. 完成所有练习后，挑战链表实现的贪吃蛇游戏！

技巧：
  • 前10道题重点是修复语法错误（编译验证）
  • 后40道题需要实现函数逻辑（单元测试）
  • 仔细阅读测试用例，它们展示了预期行为
  • 不要跳过练习，难度是递进的

准备好了吗？让我们开始吧！
"""

final_message = """
🎉 恭喜你完成了所有练习！

你已经掌握了C语言的核心知识：
  ✅ 变量和类型系统
  ✅ 控制流程（条件、循环）
  ✅ 函数设计
  ✅ 数组和字符串操作
  ✅ 指针和内存管理
  ✅ 结构体和自定义类型
  ✅ 文件输入输出

现在是时候挑战最终项目了！🐍

前往 exercises/08_final_project/snake/ 开始构建你的终端贪吃蛇游戏。
你将使用链表实现游戏逻辑，综合运用前面学到的所有知识点。

祝你玩得开心！如果你喜欢Cling，别忘了：
  • 给项目点个Star ⭐
  • 分享给你的朋友 📢
  • 贡献更多练习题 🤝

继续探索C语言的世界吧！
"""

[project]
name = "cling"
version = "0.1.0"
language = "c"
c_standard = "c11"

# ============================================================================
# Chapter 00: Introduction (2题) - 编译验证
# ============================================================================

[[exercises]]
name = "intro1"
dir = "00_intro"
mode = "compile"
hint = """
在C语言中，每条语句都必须以分号(;)结尾。
检查 printf 语句后面是否有分号。
"""

[[exercises]]
name = "intro2"
dir = "00_intro"
mode = "compile"
hint = """
这个程序有多个语法错误：
1. 检查所有的括号是否配对
2. 检查是否缺少分号
3. 检查 main 函数的声明是否正确
"""

# ============================================================================
# Chapter 01: Variables & Types (6题) - 编译验证→测试混合
# ============================================================================

[[exercises]]
name = "variables1"
dir = "01_variables"
mode = "compile"
hint = """
变量在使用前必须先声明。声明格式：
  类型 变量名;
例如：int x;
"""

[[exercises]]
name = "variables2"
dir = "01_variables"
mode = "compile"
hint = """
变量可以在声明时初始化：
  int x = 5;
未初始化的变量包含不确定的值，使用它们会导致未定义行为。
"""

[[exercises]]
name = "variables3"
dir = "01_variables"
mode = "test"
hint = """
C语言有多种基本类型：
  • int - 整数
  • float - 单精度浮点数
  • double - 双精度浮点数
  • char - 字符

查看测试用例，了解每个函数应该返回什么类型的值。
"""

[[exercises]]
name = "variables4"
dir = "01_variables"
mode = "test"
hint = """
不同的整数类型有不同的取值范围：
  • char: -128 到 127 (1字节)
  • short: -32768 到 32767 (2字节)
  • int: 通常是 -2147483648 到 2147483647 (4字节)
  • long: 至少和int一样大

查看 <limits.h> 获取准确的范围常量。
"""

[[exercises]]
name = "variables5"
dir = "01_variables"
mode = "test"
hint = """
const 关键字用于声明常量，一旦初始化就不能修改：
  const int MAX = 100;

类型转换可以是隐式的或显式的：
  double d = 3.14;
  int i = (int)d;  // 显式转换，i = 3
"""

[[exercises]]
name = "variables6"
dir = "01_variables"
mode = "test"
hint = """
sizeof 运算符返回类型或变量占用的字节数。
注意：sizeof 的结果是 size_t 类型（无符号整数）。

示例：
  sizeof(int)     // 通常是 4
  sizeof(double)  // 通常是 8
  int arr[10];
  sizeof(arr)     // 40 (假设int是4字节)
"""

[[exercises]]
name = "variables7"
dir = "01_variables"
mode = "test"
hint = """
整数除法和求余运算：
  • 整数除法丢弃小数部分：10 / 3 = 3
  • 求余运算返回余数：10 % 3 = 1
  • 验证关系：a = (a / b) * b + (a % b)

可以使用指针参数从函数返回多个值。
"""

[[exercises]]
name = "variables8"
dir = "01_variables"
mode = "test"
hint = """
类型转换技巧：
  • 四舍五入：value + 0.5 再转为int
  • 温度转换要注意整数除法问题
  • 使用 9.0/5 而不是 9/5 确保浮点运算
  
温度公式：
  F = C * 9/5 + 32
  C = (F - 32) * 5/9
"""

# ============================================================================
# Chapter 02: Control Flow (8题) - 测试驱动
# ============================================================================

[[exercises]]
name = "if1"
dir = "02_control_flow"
mode = "test"
hint = """
if 语句的基本语法：
  if (条件) {
      // 条件为真时执行
  } else {
      // 条件为假时执行
  }

在C中，0是假，非0都是真。
"""

[[exercises]]
name = "if2"
dir = "02_control_flow"
mode = "test"
hint = """
可以使用 else if 处理多个条件：
  if (条件1) {
      // ...
  } else if (条件2) {
      // ...
  } else {
      // ...
  }

逻辑运算符：&& (与), || (或), ! (非)
"""

[[exercises]]
name = "switch1"
dir = "02_control_flow"
mode = "test"
hint = """
switch 语句适合处理多个离散值：
  switch (表达式) {
      case 值1:
          // ...
          break;
      case 值2:
          // ...
          break;
      default:
          // ...
  }

不要忘记 break，否则会"fall through"到下一个case！
"""

[[exercises]]
name = "loops1"
dir = "02_control_flow"
mode = "test"
hint = """
for 循环的三个部分：初始化、条件、更新
  for (int i = 0; i < 10; i++) {
      // 循环体
  }

相当于：
  int i = 0;
  while (i < 10) {
      // 循环体
      i++;
  }
"""

[[exercises]]
name = "loops2"
dir = "02_control_flow"
mode = "test"
hint = """
while 循环在条件为真时重复执行：
  while (条件) {
      // 循环体
  }

do-while 至少执行一次：
  do {
      // 循环体
  } while (条件);
"""

[[exercises]]
name = "loops3"
dir = "02_control_flow"
mode = "test"
hint = """
break - 立即退出循环
continue - 跳过本次循环，进入下一次迭代

嵌套循环时，break/continue只影响最内层循环。
"""

[[exercises]]
name = "loops4"
dir = "02_control_flow"
mode = "test"
hint = """
do-while 循环的特点：
  do {
      // 循环体
  } while (条件);

与while的区别：至少执行一次，再检查条件。
适用场景：输入验证、菜单选择等。
"""

[[exercises]]
name = "loops5"
dir = "02_control_flow"
mode = "test"
hint = """
continue 语句的使用场景：
  • 跳过特定条件的元素
  • 简化if-else嵌套结构
  • 提前进入下一次迭代

示例：
  for (int i = 0; i < n; i++) {
      if (i % 2 == 0) continue;  // 跳过偶数
      // 处理奇数
  }
"""

# ============================================================================
# Chapter 03: Functions (8题) - 测试驱动
# ============================================================================

[[exercises]]
name = "functions1"
dir = "03_functions"
mode = "test"
hint = """
函数的基本结构：
  返回类型 函数名(参数列表) {
      // 函数体
      return 值;
  }

如果函数不返回值，使用 void 作为返回类型。
"""

[[exercises]]
name = "functions2"
dir = "03_functions"
mode = "test"
hint = """
函数参数在C中是按值传递的，即传递的是副本。
修改参数不会影响原始变量。

多个参数用逗号分隔：
  int add(int a, int b) {
      return a + b;
  }
"""

[[exercises]]
name = "functions3"
dir = "03_functions"
mode = "test"
hint = """
函数可以在使用前声明（函数原型），在后面定义：
  int add(int a, int b);  // 声明
  
  int main() {
      add(1, 2);  // 使用
  }
  
  int add(int a, int b) {  // 定义
      return a + b;
  }
"""

[[exercises]]
name = "functions4"
dir = "03_functions"
mode = "test"
hint = """
return 语句：
1. 返回一个值给调用者
2. 立即结束函数执行

void 函数可以使用 return; （不带值）来提前返回。
"""

[[exercises]]
name = "functions5"
dir = "03_functions"
mode = "test"
hint = """
函数可以有多个返回语句，但只会执行其中一个。
确保所有可能的执行路径都有返回值。

编译器可能会警告"控制到达非void函数末尾"。
"""

[[exercises]]
name = "functions6"
dir = "03_functions"
mode = "test"
hint = """
变量作用域：
  • 局部变量 - 在函数内声明，只在函数内可见
  • 全局变量 - 在函数外声明，所有函数可见

避免过度使用全局变量，它们会使代码难以理解和维护。
"""

[[exercises]]
name = "functions7"
dir = "03_functions"
mode = "test"
hint = """
递归函数会调用自己。递归必须有：
1. 基准情况（终止条件）
2. 递归情况（向基准情况靠近）

例如阶乘：
  int factorial(int n) {
      if (n <= 1) return 1;  // 基准
      return n * factorial(n - 1);  // 递归
  }
"""

[[exercises]]
name = "functions8"
dir = "03_functions"
mode = "test"
hint = """
递归入门练习：
  • sum_recursive: 思考如何把1到n的和拆解为：n + (1到n-1的和)
  • countdown_product: 类似阶乘，但从n开始倒数
  • count_digits: 每次递归减少一位数字（除以10）

递归三要素：
  1. 基准情况（最简单的情况直接返回）
  2. 递归情况（调用自己解决更小的问题）
  3. 确保问题规模在缩小（避免无限递归）
"""

# ============================================================================
# Chapter 04: Arrays & Strings (8题) - 测试驱动
# ============================================================================

[[exercises]]
name = "arrays1"
dir = "04_arrays"
mode = "test"
hint = """
数组声明和初始化：
  int arr[5];  // 声明包含5个int的数组
  int arr[5] = {1, 2, 3, 4, 5};  // 声明并初始化
  int arr[] = {1, 2, 3};  // 自动推断大小为3

数组索引从0开始！
"""

[[exercises]]
name = "arrays2"
dir = "04_arrays"
mode = "test"
hint = """
访问数组元素使用方括号：
  arr[0]  // 第一个元素
  arr[4]  // 第五个元素

注意：C不检查数组边界！访问arr[5]（对于大小为5的数组）
是未定义行为，可能导致程序崩溃。
"""

[[exercises]]
name = "arrays3"
dir = "04_arrays"
mode = "test"
hint = """
遍历数组通常使用for循环：
  int arr[5] = {1, 2, 3, 4, 5};
  for (int i = 0; i < 5; i++) {
      printf("%d ", arr[i]);
  }

可以使用sizeof来获取数组大小：
  int len = sizeof(arr) / sizeof(arr[0]);
"""

[[exercises]]
name = "arrays4"
dir = "04_arrays"
mode = "test"
hint = """
多维数组声明：
  int matrix[3][4];  // 3行4列

初始化：
  int matrix[2][3] = {
      {1, 2, 3},
      {4, 5, 6}
  };

访问：matrix[0][0] 是第一行第一列
"""

[[exercises]]
name = "strings1"
dir = "04_arrays"
mode = "test"
hint = """
C中的字符串是以空字符'\\0'结尾的字符数组：
  char str[] = "Hello";  // 自动包含'\\0'
  // 等价于: char str[] = {'H', 'e', 'l', 'l', 'o', '\\0'};

字符串字面量 "Hello" 的类型是 char[6]（包含'\\0'）
"""

[[exercises]]
name = "strings2"
dir = "04_arrays"
mode = "test"
hint = """
字符和字符串的区别：
  'A'   // 字符，类型是 int (ASCII值)
  "A"   // 字符串，类型是 char[2] (包含'A'和'\\0')

单引号用于字符，双引号用于字符串。
"""

[[exercises]]
name = "strings3"
dir = "04_arrays"
mode = "test"
hint = """
常用字符串函数（需要 #include <string.h>）：
  strlen(s)       - 返回字符串长度（不包括'\\0')
  strcpy(dst, src) - 复制字符串
  strcat(dst, src) - 连接字符串
  strcmp(s1, s2)  - 比较字符串

注意：这些函数不检查缓冲区大小，使用时要小心！
"""

[[exercises]]
name = "strings4"
dir = "04_arrays"
mode = "test"
hint = """
strcmp 返回值：
  < 0  如果 s1 < s2
  = 0  如果 s1 == s2
  > 0  如果 s1 > s2

不能用 == 比较字符串！要用 strcmp：
  if (strcmp(s1, s2) == 0) {  // 字符串相等
      // ...
  }
"""

# ============================================================================
# Chapter 05: Pointers (10题) - 测试驱动，重点章节
# ============================================================================

[[exercises]]
name = "pointers1"
dir = "05_pointers"
mode = "test"
hint = """
指针是存储内存地址的变量：
  int x = 42;
  int *p = &x;  // p存储x的地址

运算符：
  & - 取地址运算符
  * - 解引用运算符（访问指针指向的值）
"""

[[exercises]]
name = "pointers2"
dir = "05_pointers"
mode = "test"
hint = """
解引用指针访问它指向的值：
  int x = 42;
  int *p = &x;
  printf("%d", *p);  // 输出 42
  *p = 100;          // 修改x的值为100

*p 可以像普通变量一样使用。
"""

[[exercises]]
name = "pointers3"
dir = "05_pointers"
mode = "test"
hint = """
指针算术运算：
  int arr[5] = {1, 2, 3, 4, 5};
  int *p = arr;  // p指向arr[0]
  p++;           // p现在指向arr[1]
  p += 2;        // p现在指向arr[3]

p + 1 移动sizeof(int)字节，而不是1字节！
"""

[[exercises]]
name = "pointers4"
dir = "05_pointers"
mode = "test"
hint = """
指针和数组的关系：
  int arr[5];
  int *p = arr;  // arr自动转换为指向第一个元素的指针
  
  arr[i] 等价于 *(arr + i)
  p[i]   等价于 *(p + i)

数组名在大多数情况下会退化为指针。
"""

[[exercises]]
name = "pointers5"
dir = "05_pointers"
mode = "test"
hint = """
数组作为函数参数时会退化为指针：
  void func(int arr[]) { ... }
  // 等价于
  void func(int *arr) { ... }

因此需要单独传递数组大小：
  void func(int *arr, int size) { ... }
"""

[[exercises]]
name = "pointers6"
dir = "05_pointers"
mode = "test"
hint = """
通过指针修改函数外的变量（模拟引用传递）：
  void swap(int *a, int *b) {
      int temp = *a;
      *a = *b;
      *b = temp;
  }
  
  int x = 1, y = 2;
  swap(&x, &y);  // 传递地址
"""

[[exercises]]
name = "pointers7"
dir = "05_pointers"
mode = "test"
hint = """
函数指针存储函数的地址：
  int add(int a, int b) { return a + b; }
  
  int (*func_ptr)(int, int) = add;  // 声明并初始化
  int result = func_ptr(3, 4);      // 调用
  // 或
  int result = (*func_ptr)(3, 4);

函数名会自动转换为指针。
"""

[[exercises]]
name = "pointers8"
dir = "05_pointers"
mode = "test"
hint = """
函数指针可以作为参数传递（回调函数）：
  void apply(int *arr, int size, int (*func)(int)) {
      for (int i = 0; i < size; i++) {
          arr[i] = func(arr[i]);
      }
  }

这允许函数的行为参数化。
"""

[[exercises]]
name = "pointers9"
dir = "05_pointers"
mode = "test"
hint = """
二级指针（指向指针的指针）：
  int x = 42;
  int *p = &x;
  int **pp = &p;
  
  printf("%d", **pp);  // 输出 42

常用于修改指针本身，或动态二维数组。
"""

[[exercises]]
name = "pointers10"
dir = "05_pointers"
mode = "test"
hint = """
NULL指针：
  int *p = NULL;  // 空指针，不指向任何有效内存

使用前检查指针是否为NULL：
  if (p != NULL) {
      *p = 42;  // 安全
  }

解引用NULL指针会导致段错误（Segmentation Fault）。
"""

[[exercises]]
name = "pointers11"
dir = "05_pointers"
mode = "test"
hint = """
指针与数组的等价性：
  int arr[5];
  arr[i] == *(arr + i)  // 始终成立
  
数组名arr就是指向第一个元素的指针。
指针算术：arr + i 指向第i个元素的地址。

两种访问方式完全等价：
  • 数组语法：arr[i]
  • 指针语法：*(arr + i)
"""

# ============================================================================
# Chapter 06: Structs & Memory (8题) - 测试驱动
# ============================================================================

[[exercises]]
name = "structs1"
dir = "06_structs"
mode = "test"
hint = """
结构体定义和使用：
  struct Point {
      int x;
      int y;
  };
  
  struct Point p;
  p.x = 10;
  p.y = 20;

使用.运算符访问成员。
"""

[[exercises]]
name = "structs2"
dir = "06_structs"
mode = "test"
hint = """
结构体初始化：
  struct Point p = {10, 20};  // 按顺序初始化
  struct Point p = {.x = 10, .y = 20};  // 指定成员（C99）

通过指针访问结构体成员：
  struct Point *p = &pt;
  p->x = 10;  // 等价于 (*p).x = 10
"""

[[exercises]]
name = "structs3"
dir = "06_structs"
mode = "test"
hint = """
结构体可以包含数组、指针、甚至其他结构体：
  struct Person {
      char name[50];
      int age;
      struct Date birthday;
  };

结构体赋值会复制所有成员。
"""

[[exercises]]
name = "structs4"
dir = "06_structs"
mode = "test"
hint = """
typedef 为类型创建别名：
  typedef struct {
      int x;
      int y;
  } Point;
  
  Point p;  // 无需写 struct

这样可以简化代码，使类型名更简洁。
"""

[[exercises]]
name = "memory1"
dir = "06_structs"
mode = "test"
hint = """
动态内存分配：
  int *p = (int *)malloc(sizeof(int) * 10);  // 分配10个int
  if (p == NULL) {
      // 内存分配失败
  }
  
  // 使用p...
  
  free(p);  // 释放内存
  p = NULL;  // 防止悬空指针

始终检查malloc返回值！
"""

[[exercises]]
name = "memory2"
dir = "06_structs"
mode = "test"
hint = """
calloc vs malloc：
  malloc(size)      - 分配但不初始化
  calloc(n, size)   - 分配并初始化为0
  
  int *p1 = malloc(10 * sizeof(int));  // 未初始化
  int *p2 = calloc(10, sizeof(int));   // 全部初始化为0

calloc对于数组特别有用。
"""

[[exercises]]
name = "memory3"
dir = "06_structs"
mode = "test"
hint = """
常见内存错误：
1. 内存泄漏 - 分配后忘记释放
2. 悬空指针 - 使用已释放的内存
3. 重复释放 - 对同一指针调用free多次
4. 缓冲区溢出 - 访问越界

realloc 调整已分配内存的大小：
  p = realloc(p, new_size);
"""

[[exercises]]
name = "memory4"
dir = "06_structs"
mode = "test"
hint = """
实现动态数据结构的关键步骤：
1. 在结构体中保存data, size, capacity
2. 初始分配小容量（如8）
3. 添加元素时检查容量
4. 容量不足时使用realloc扩容（通常翻倍）
5. 最后释放所有动态内存

注意：
  • realloc可能移动内存，要更新指针
  • 始终先检查malloc/realloc返回值
  • 释放时先释放内部指针，再释放结构体
"""

# ============================================================================
# Chapter 07: File I/O (4题) - 测试驱动
# ============================================================================

[[exercises]]
name = "file1"
dir = "07_file_io"
mode = "test"
hint = """
打开和关闭文件：
  FILE *fp = fopen("file.txt", "r");  // 打开读取
  if (fp == NULL) {
      // 错误处理
  }
  
  // 使用文件...
  
  fclose(fp);

模式："r"(读), "w"(写), "a"(追加), "r+"(读写)
"""

[[exercises]]
name = "file2"
dir = "07_file_io"
mode = "test"
hint = """
格式化读写：
  fprintf(fp, "数字: %d\n", num);  // 写入
  fscanf(fp, "%d", &num);          // 读取

fgets 读取一行：
  char line[100];
  fgets(line, sizeof(line), fp);

fputs 写入字符串：
  fputs("Hello\n", fp);
"""

[[exercises]]
name = "file3"
dir = "07_file_io"
mode = "test"
hint = """
二进制文件操作：
  fwrite(ptr, size, count, fp);  // 写入
  fread(ptr, size, count, fp);   // 读取

例如：
  int arr[10];
  fwrite(arr, sizeof(int), 10, fp);  // 写入10个int
  fread(arr, sizeof(int), 10, fp);   // 读取10个int
"""

[[exercises]]
name = "file4"
dir = "07_file_io"
mode = "test"
hint = """
错误处理：
  if (ferror(fp)) {
      // 发生错误
  }
  
  if (feof(fp)) {
      // 到达文件末尾
  }

clearerr(fp) 清除错误标志。

始终检查文件操作的返回值！
"""

# ============================================================================
# Chapter 08: Final Project (1个项目) - 综合测试
# ============================================================================

[[exercises]]
name = "snake_logic"
dir = "08_final_project/snake"
mode = "test"
hint = """
这是最终项目！你需要实现贪吃蛇游戏的核心逻辑。

框架已提供：
  ✅ UI渲染 (snake_ui.c)
  ✅ 键盘输入 (snake_input.c)
  ✅ 游戏主循环 (snake_main.c)

你需要实现 (snake_logic.c)：
  1. snake_node_create() - 创建链表节点
  2. snake_insert_head() - 在链表头插入
  3. snake_remove_tail() - 删除链表尾
  4. snake_check_collision() - 碰撞检测
  5. snake_move() - 移动蛇
  6. food_generate() - 生成食物
  7. game_update() - 更新游戏状态
  8. snake_free() - 释放链表

查看 snake.h 了解数据结构。运行 'make snake_test' 查看测试结果。
"""
